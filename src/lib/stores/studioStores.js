import { writable } from 'svelte/store';

export const crewStore = writable([]);
export const projectsStore = writable([]);
export const heroImageStore = writable([]);
export const sectionContentStore = writable([]);
